name: windows_example
description: "libvips_ffi Windows example app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  libvips_ffi_windows: ^0.1.0
  libvips_flutter_example_base:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
